using UnityEngine;
using UnityEngine.UI;
//using Npgsql;

public class NewBehaviourScript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //NpgsqlConnection npgsqlConnection = new NpgsqlConnection("Host=127.0.0.1;Database=akshaysdemodb;Username=postgres;Password=P@ssword123");
        //npgsqlConnection.Open();
        //NpgsqlCommand npgsqlCommand = new NpgsqlCommand("select * from recipes", npgsqlConnection);
        //NpgsqlDataReader npgsqlDataReader = npgsqlCommand.ExecuteReader();
        //while (npgsqlDataReader.Read())
        //{
        //    gameObject.GetComponent<Text>().text = npgsqlDataReader["name"].ToString();
        //    break;
        //}
        //npgsqlDataReader.Close();
        //npgsqlConnection.Close();
    }
}
